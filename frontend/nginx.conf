server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: let client-side routing (if any is added later) own
    # unknown paths instead of nginx 404ing them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
